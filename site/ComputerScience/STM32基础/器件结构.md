# 器件逻辑结构

## Flash

- Flash（最大擦除单位）->块->扇区（最小擦除单位）->页（最大写单位）->字节（最小读写单位）
- Flash不可以在忙状态时操作
- Flash擦写：写0、擦除1（擦除后自动变为1），进入忙状态
  - 所以每次写入前必须擦除为全1再写0（所以有写使能）
  - 写入内容超过一页，会回到页首覆盖写入
  - Flash写入后==进入忙状态==
- Flash读：不进入忙状态
  - Flash读出后==不进入忙状态==